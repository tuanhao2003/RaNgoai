##### 1 : Khang - Hệ Thống xếp nhóm trong lớp học #####
Tôi là nhân viên công nghệ thông tin đang làm việc tại một công ty công nghệ. Tôi đang phát triển một hệ thống để xếp nhóm trong lớp bằng ngôn ngữ Java. Trong quá trình phát triển, tôi đã tạo ra một chương trình mô phỏng hệ thống. Tôi muốn kiểm tra xem đoạn mã đã hoạt động chính xác hay chưa. Việc kiểm tra phải được làm kỹ lưỡng vì đoạn mã này sẽ là tiền đề cho hệ thống của tôi. Đoạn mã của tôi hoạt động bằng cách nhận vào một danh sách sinh viên sau đó duyệt danh sách đó và chia các nhân viên vào các nhóm đều nhau. Sinh viên có các thuộc tính như MSSV, Họ tên và lớp.
Các ràng buộc mà đoạn mã cần phải tuân theo là: MSSV phải và nó phải là duy nhất, họ tên và lớp không được để trống.
Hãy kiểm tra kết quả của đoạn mã dưới đây dự trên các trường hợp kiểm thử được cung cấp và giải thích chi tiết cho từng trường hợp.
Dưới đây là đoạn mã của tôi:
```java
import java.util.ArrayList;
import java.util.List;

class Student {
    String studentID;
    String name;
    String className;

    public Student(String studentID, String name, String className) {
        if (studentID == null || studentID.isEmpty()) {
            throw new IllegalArgumentException("MSSV không được để trống.");
        }
        if (name == null || name.isEmpty()) {
            throw new IllegalArgumentException("Họ tên không được để trống.");
        }
        if (className == null || className.isEmpty()) {
            throw new IllegalArgumentException("Lớp không được để trống.");
        }
        this.studentID = studentID;
        this.name = name;
        this.className = className;
    }
}

class StudentGroupAllocator {
    public static void main(String[] args) {
       
    }
}
```
Dưới đây là các trường hợp kiểm thử:
```java
        // TH 1
        List<Student> students = new ArrayList<>();
        students.add(new Student("SV001", "John", "Class A"));
        students.add(new Student("SV002", "Jane", "Class B"));
        students.add(new Student("SV003", "Doe", "Class C"));

        for (Student student : students) {
            System.out.println("Student: " + student.name + ", Class: " + student.className);
        }

        // TH 2
        students = new ArrayList<>();
        students.add(new Student("SV004", "Sew", "Class A"));
        students.add(new Student("SV005", "Jee", "Class B"));
        students.add(new Student("SV006", "Done", "Class C"));

        for (Student student : students) {
            System.out.println("Student: " + student.name + ", Class: " + student.className);
        }

        // TH 3
        students = new ArrayList<>();
        for (Student student : students) {
            System.out.println("Student: " + student.name + ", Class: " + student.className);
        }
```